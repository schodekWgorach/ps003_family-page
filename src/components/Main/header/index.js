import React from 'react';
import './header.scss';

const Header = () => {
    return (
        <div className="header_box">
            Header w header
        </div>
    );
};

export default Header;